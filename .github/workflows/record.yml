name: Record Stream

on:
  workflow_dispatch:
    inputs:
      stream_url:
        description: 'Target Room Web URL'
        required: true
        default: ''

jobs:
  record:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg
          python -m pip install --upgrade pip
          python -m pip install -U "yt-dlp[default,curl-cffi]"

      - name: Download Stream
        run: |
          timeout --preserve-status 21000s yt-dlp "${{ github.event.inputs.stream_url }}" \
            -o "raw_recording_%(epoch)s.ts" \
            --impersonate chrome \
            --hls-use-mpegts \
            --fragment-retries infinite \
            --retry-sleep 5 \
            --retries 10 || true

      - name: Fix and Clean Video Container
        if: always()
        run: |
          for f in raw_recording_*.ts; do
            [ -e "$f" ] || continue
            ffmpeg -err_detect ignore_err -i "$f" -c copy "fixed_${f%.ts}.mp4"
          done

      - name: Upload Video Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: recorded-stream
          path: fixed_*.mp4
          retention-days: 1
